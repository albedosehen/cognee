Generic single-database configuration with an async dbapi.

`./versions_old` is the alembic db migrations from pre-forked cognee. I gave up trying to fix all the issues I kept running into and instead wrote a new one in `./versions`
