# ============================================================
# Cognee Frontend Environment Configuration
# ============================================================
# This template documents ALL environment variables used by
# cognee-frontend. The frontend uses ONLY API calls - it has
# NO direct database connections.
#
# Copy this file to .env.local for local development.
# ============================================================

# ------------------------------------------------------------
# Next.js Configuration
# ------------------------------------------------------------
# Disable Next.js telemetry for privacy and performance
# Set to 1 to disable telemetry collection
NEXT_TELEMETRY_DISABLED=1

# ------------------------------------------------------------
# Backend API Configuration
# ------------------------------------------------------------
# Primary Cognee backend API URL
# Docker: Maps to container port 8000, exposed as host port 8001
# Standalone: Typically http://localhost:8000
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8001

# Cloud API URL (used for cloud-specific operations)
# In local development, this typically points to the same backend
NEXT_PUBLIC_CLOUD_API_URL=http://localhost:8001

# ------------------------------------------------------------
# MCP (Model Context Protocol) Server Configuration
# ------------------------------------------------------------
# Cognee MCP server API URL
# Docker: Maps to container port 8000, exposed as host port 9000
# Used for IDE integration tools (Cursor, Claude Desktop, VSCode)
NEXT_PUBLIC_MCP_API_URL=http://localhost:9000

# ------------------------------------------------------------
# Authentication & Authorization
# ------------------------------------------------------------
# Enable/disable Auth0 authorization
# Set to "true" to enable Auth0, "false" to disable
# Default: false (local development without Auth0)
USE_AUTH0_AUTHORIZATION=false

# Authentication token cookie name
# Used when storing/retrieving auth tokens in cookies
# Defaults to "cognee_token" if not specified
AUTH_TOKEN_COOKIE_NAME=cognee_token

# Auth0 Configuration (Required when USE_AUTH0_AUTHORIZATION=true)
# Uncomment and configure these variables when enabling Auth0 authentication:
# AUTH0_SECRET=your-auth0-secret-here
# AUTH0_BASE_URL=http://localhost:3000
# AUTH0_ISSUER_BASE_URL=https://your-tenant.auth0.com
# AUTH0_CLIENT_ID=your-auth0-client-id
# AUTH0_CLIENT_SECRET=your-auth0-client-secret

# ------------------------------------------------------------
# API Keys & Secrets
# ------------------------------------------------------------
# Cogwit API key (if using Cogwit cloud services)
# Leave empty for local development
NEXT_PUBLIC_COGWIT_API_KEY=

# ------------------------------------------------------------
# Environment Flags
# ------------------------------------------------------------
# Determines if running in cloud environment
# Set to "true" for cloud deployments, "false" for local
# This affects which API URLs are used internally
NEXT_PUBLIC_IS_CLOUD_ENVIRONMENT=false

# ============================================================
# Database Architecture Notes
# ============================================================
# Frontend → Backend API only (no direct database connections)
#
# Backend (cognee service) actual configuration in docker-compose:
#   - Vector DB: Qdrant (http://local-llm-qdrant:6333)
#   - Relational DB: SQLite (default, no explicit config needed)
#   - Graph DB: NONE configured by default
#   - LLM Provider: Ollama (http://local-llm-ollama:11434)
#   - Embedding Provider: Ollama (http://local-llm-ollama:11434)
#
# Optional databases (defined but require profile activation):
#   - Neo4j: Available with profile "neo4j" or "cognee-db"
#   - ChromaDB: Available with profile "chromadb" or "cognee-db"
#   - PostgreSQL: Available with profile "postgres" or "cognee-db"
#   - Redis: Available with profile "redis" or "cognee-db"
#
# To use optional databases, see docker-compose.yml backend
# environment configuration and activate appropriate profiles.
# ============================================================
